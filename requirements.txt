# MMX Enterprise (v5.4.3) - runtime requirements
# NOTE: Keep versions loose for easier installation on Debian/GCP images.

fastapi>=0.110
uvicorn[standard]>=0.27

# Dashboard
streamlit>=1.32
matplotlib>=3.7

# Data
pandas>=2.0
numpy>=1.24
scipy>=1.10
pyyaml>=6.0
python-dateutil>=2.8

# Modeling / Bayesian SEM
pymc>=5.10
pytensor>=2.18
arviz>=0.17

# Reports (PDF)
reportlab>=4.0

# PyMC stack sometimes pulls numba; ensure compatibility on Debian images
numba<0.60