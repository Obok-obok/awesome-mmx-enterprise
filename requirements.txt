# MMX Enterprise - runtime requirements
# Streamlit Cloud currently uses Python 3.13 by default unless runtime.txt is provided.
# We intentionally avoid numba/llvmlite because they do not support Python 3.13 on manylinux yet.

fastapi>=0.110
uvicorn[standard]>=0.27

# Dashboard
streamlit>=1.32
matplotlib>=3.7

# Data / Math
pandas>=2.0
numpy>=1.24
scipy>=1.10
pyyaml>=6.0
python-dateutil>=2.8

# Modeling / Bayesian SEM
pymc>=5.10
pytensor>=2.18
arviz>=0.17

# Reports (PDF)
reportlab>=4.0
